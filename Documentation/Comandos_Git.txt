// Configurar git con archivos e ignorar los necesarios

git init

crear archivo .gitingnore y dentro agrega solo nombre de los archivos con punto y carpetas

Comando proporcionados por github

ingresar usuario antes de
git config --global user.name "xfiberex"
git config --global user.email "rickystrong79@gmail.com"

git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/xfiberex/agenicaviajesnode.git
git push -u origin main

Al hacer cambios en el codigo
git status

para confirmar cambios
git commit -m "Descripción breve de los cambios"

para subir los cambios al repositorio
git push origin main
